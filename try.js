<div id="cardchip-container-${item.id}" class="custom-dropdown" data-id="${item.id}">
    <button id="carchip-toggle-${item.id}" type="button" class="dropdown-toggle">
        + Add category
    </button>
    <div id="cardchip-menu-${item.id}" class="dropdown-menu"></div>
</div>
